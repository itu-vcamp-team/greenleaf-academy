services:
  # --- BACKEND ---
  - type: web
    name: greenleaf-backend
    runtime: python
    branch: main
    rootDir: backend
    buildCommand: pip install -r requirements.txt && alembic upgrade head && python seed_initial_data.py
    startCommand: uvicorn src.app:app --host 0.0.0.0 --port 10000
    plan: free
    envVars:
      - key: APP_ENV
        value: production
      - key: PYTHON_VERSION
        value: 3.12.8
      - key: JWT_SECRET_KEY
        sync: false
      - key: DATABASE_URL
        fromDatabase:
          name: greenleaf-postgres
          property: connectionString
      - key: REDIS_URL
        fromService:
          name: greenleaf-redis
          type: redis
          property: connectionString
      - key: RESEND_API_KEY
        sync: false
      - key: MAIL_FROM_ADDRESS
        value: noreply@greenleafakademi.com
      - key: MAIL_FROM_NAME
        value: Greenleaf Akademi
      - key: CORS_ALLOWED_ORIGIN_REGEX
        value: 'https?://([a-z0-9-]+\.)*greenleafakademi\.com'
      - key: FRONTEND_URL
        value: https://tr.greenleafakademi.com

  # --- FRONTEND ---
  - type: web
    name: greenleaf-frontend
    runtime: node
    branch: main
    rootDir: frontend
    buildCommand: npm ci && npm run build
    startCommand: npm start
    plan: free
    envVars:
      - key: NEXT_PUBLIC_BACKEND_URL
        value: https://greenleaf-backend-wkfv.onrender.com

  # --- REDIS ---
  - type: redis
    name: greenleaf-redis
    plan: free
    maxmemoryPolicy: allkeys-lru
    ipAllowList:
      - source: 0.0.0.0/0
        description: allow all

databases:
  - name: greenleaf-postgres
    databaseName: greenleaf_db
    user: greenleaf
    plan: free