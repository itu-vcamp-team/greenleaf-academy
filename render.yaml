services:
  # --- BACKEND (Single-tenant: one deployment per server) ---
  - type: web
    name: greenleaf-backend
    runtime: python
    branch: main
    rootDir: backend
    # seed_initial_data.py must be run manually ONCE after fresh deploy:
    #   render run greenleaf-backend -- python seed_initial_data.py
    buildCommand: pip install -r requirements.txt && alembic upgrade head
    startCommand: uvicorn src.app:app --host 0.0.0.0 --port 10000
    plan: starter
    disk:
      name: greenleaf-uploads
      mountPath: /var/data/uploads
      sizeGB: 1
    envVars:
      - key: APP_ENV
        value: production
      - key: PYTHON_VERSION
        value: 3.12.11
      - key: PYTHONPATH
        value: /opt/render/project/src
      - key: JWT_SECRET_KEY
        sync: false
      - key: DATABASE_URL
        fromDatabase:
          name: greenleaf-postgres
          property: connectionString
      - key: REDIS_URL
        fromService:
          name: greenleaf-redis
          type: redis
          property: connectionString
      - key: AWS_ACCESS_KEY_ID
        sync: false
      - key: AWS_SECRET_ACCESS_KEY
        sync: false
      - key: AWS_SES_REGION
        value: eu-north-1
      - key: MAIL_FROM_ADDRESS
        value: noreply@greenleafakademi.com
      - key: MAIL_FROM_NAME
        value: Greenleaf Akademi
      - key: UPLOAD_DIR
        value: /var/data/uploads
      - key: CORS_ALLOWED_ORIGIN_REGEX
        value: 'https?://([a-z0-9-]+\.)*greenleafakademi\.com'
      - key: FRONTEND_URL
        value: https://greenleafakademi.com
      - key: TURNSTILE_SECRET_KEY
        sync: false

  # --- ARQ WORKER ---
  - type: worker
    name: greenleaf-arq-worker
    runtime: python
    branch: main
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: arq src.worker.WorkerSettings
    plan: starter
    envVars:
      - key: APP_ENV
        value: production
      - key: PYTHONPATH
        value: /opt/render/project/src
      - key: DATABASE_URL
        fromDatabase:
          name: greenleaf-postgres
          property: connectionString
      - key: REDIS_URL
        fromService:
          name: greenleaf-redis
          type: redis
          property: connectionString
      - key: AWS_ACCESS_KEY_ID
        sync: false
      - key: AWS_SECRET_ACCESS_KEY
        sync: false
      - key: AWS_SES_REGION
        value: eu-north-1

  # --- FRONTEND (Single-tenant: one deployment per server) ---
  - type: web
    name: greenleaf-frontend
    runtime: node
    branch: main
    rootDir: frontend
    buildCommand: npm ci && npm run build
    startCommand: npm start
    plan: free
    envVars:
      - key: NEXT_PUBLIC_BACKEND_URL
        value: https://greenleaf-backend-wkfv.onrender.com
      - key: NEXT_PUBLIC_TURNSTILE_SITE_KEY
        value: 0x4AAAAAADDnJNGyXUKu4w1k

  # --- REDIS ---
  - type: redis
    name: greenleaf-redis
    plan: free
    maxmemoryPolicy: allkeys-lru
    ipAllowList:
      - source: 0.0.0.0/0
        description: allow all

databases:
  # Fresh database (new schema, no tenants table)
  - name: greenleaf-postgres
    databaseName: greenleaf_db
    user: greenleaf
    plan: free

# ─── Superadmin service is deployed SEPARATELY ────────────────────────────────
# See: superadmin/backend/ and superadmin/frontend/
# Superadmin has its own render services and its own database.
